// Import styles
import { StyledLoader } from "./styles";

const Loader = () => {
  return (
    <StyledLoader>
      <span className="loader"></span>
    </StyledLoader>
  );
}

export default Loader;